import React from 'react'
import {Container} from 'react-bootstrap'

const LinkedList = () => {
    return (
        <Container>
            <h2>Linked List</h2>
        </Container>
    )
}

export default LinkedList