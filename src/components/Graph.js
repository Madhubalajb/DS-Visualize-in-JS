import React from 'react'
import {Container} from 'react-bootstrap'

const Graph = () => {
    return (
        <Container>
            <h2>Graph</h2>
        </Container>
    )
}

export default Graph